[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ycf"
version = "0.0.1"
authors = [
  { name="UMadd" }
]
description = "A script to help solve problems"
readme = "README.md"
requires-python = ">=3.7"


